---
- name: Creates directory
  file:
    path: ~/server
    state: directory

- name: "extract artifact."
  unarchive:
    src: "files/artifact.tar.gz"
    dest: "~/server"

- name: "start server"
  become: true
  shell: |
    cd ~/server
    npm install
    pm2 stop default
    pm2 start npm -- start